import { AsyncVectorPipe } from './async-vector.pipe';

describe('AsyncVectorPipe', () => {
  it('create an instance', () => {
    const pipe = new AsyncVectorPipe();
    expect(pipe).toBeTruthy();
  });
});
